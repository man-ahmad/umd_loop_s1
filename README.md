# UMD Loop S1 Challenge - Mansoor Ahmad

You can run this with a single command, `ros2 launch robot_control_node umd_loop.launch.py` assuming all the dependencies are there, which you can check with `rosdep`. The timestamps for the commits are all out of wack because, as I explained to Didi Zhang, CTE, I had a slew
of SSH problems due to my public key suddenly not working, so I deleted the old one, made a new one and tried to manually upload the files, but they were taking too long + there
was a problem with the log folder structure. Deleted that one, re-did the SSH from scratch, setup a new key, made a new repo (this one), and finally got all the files up here. All of this is documented
with Didi Zhang. 
